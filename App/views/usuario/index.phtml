

<div class="z-depth-1 grey lighten-4 row" style="padding: 30px;">
  <div class="container">
    <h3>USUARIOS</h3>
    <?php
      if(isset($this->view->message))
      {
        echo $this->view->message;
       
      }
    ?>

    <table class="table">
        <thead>
          <tr>
            <th><a class="btn btn-primary" href="form-usuario.php" style="margin-right: 30px;">Novo</a></th>
            <th>NOME</th>
            <th>EMAIL</th>
            <th>OBRAS</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <?php foreach($this->view->usuarios as $usuario): ?>
            <td style="margin-right: 30px;" > 
                <a href="/usuarios/delete/<?php echo $usuario['id'] ?>" class="btn-floating btn-small waves-effect waves-light red right"><i class="material-icons">delete</i></a>
                <a href="/usuario/cad/<?php echo $usuario['id']?>" class="btn-floating btn-small waves-effect waves-light red right"><i class="material-icons">edit</i></a>
            </td>
            <td><?php print_r($usuario['nome']) ?></td>
            <td><?php print_r($usuario['email']) ?></td>
            <td><?php print_r($usuario['obra']); ?></td>
          </tr>
            <?php endforeach; ?>
        </tbody>
      </table>
  </div>
</div>